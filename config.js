// Total Number of Parking Spots

const totalParkingSpots = 150;

// Starting Rate per hour

const startingRate = 3;

// Percentage Rate Increases by Level

const rateIncrease = 50;

// Rate Levels

const rateLevels = ['1hr', '3hr', '6hr', 'ALL DAY'];

module.exports = {
  totalParkingSpots: totalParkingSpots,
  startingRate: startingRate,
  rateIncrease: rateIncrease,
  rateLevels: rateLevels,
};
